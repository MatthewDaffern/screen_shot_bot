import change_tracker
import time

def user_interaction():
        time_amount = input(str.join('',('Please specify the rate of checking the bot will do. \n',
                                         'Use the following example:',
                                         '3 hours'
                        )))



def time_converter(str_input):
        time_list = time_amount.split(' ')
        units_of_time_dict = dict([('second', 1),('minute', 60)('hour', 3600)('day', 86400)])
        key_list = [k for k,v in units_of_time_dict.items]
        if i in key_list:
                result = 
                return 
# finish this loop.
        

def time_delay(time_input , function_input):


def loop(time_in_minutes):
    while True:
        change_tracker.main()


if __name__ == "__main__":
    loop()
